export { OPENSEARCH_BASE_URL, OPENSEARCH_INDEX, OPENSEARCH_DEFAULT_USER } from './envs';

export const DEFAULT_PAGE_SIZE = 100;

export const SORTING_ORDER = {
  ASC: 'asc',
  DESC: 'desc',
} as const;
