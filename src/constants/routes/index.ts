export const ROUTES = {
  HOME: 'home',
  SAVED_FILTERS: 'saved-filters',
  LIBRARIES: 'libraries',
} as const;

export const PATHS = {
  HOME: '/',
  SAVED_FILTERS: '/filtros-salvos',
  LIBRARIES: '/biblioteca',
} as const;
